# SITL Quickstart (WSL2 + PX4 SITL + ROS 2 Humble)

This is the baseline “known-good” setup:
- PX4 SITL (Gazebo)
- Micro XRCE-DDS Agent (UDP 8888)
- ROS2 offboard node streaming OffboardControlMode + TrajectorySetpoint at ~20 Hz

## One-time build (ROS workspace)
